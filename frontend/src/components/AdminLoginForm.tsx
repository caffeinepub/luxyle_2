// This component is no longer used — AdminDashboard handles auth directly via useInternetIdentity.
// Kept as a stub to avoid import errors if referenced elsewhere.
export default function AdminLoginForm() {
  return null;
}
